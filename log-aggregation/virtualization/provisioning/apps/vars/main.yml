---
apps_dir: /usr/local/bin
apps:
  javaapp:
    artifacts:
      - javapp-1.0-SNAPSHOT.jar
      - config.yaml
  pythonapp:
    artifacts:
      - pythonapp.zip
  golangapp:
    artifacts:
      - golangapp
